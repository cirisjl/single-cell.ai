{
    "type": "object",
    "properties": {
      "parameters": {
        "type":"object",
        "title": "Parameters for Conversion Process",  
        "properties": {
          "output_format": { 
            "type": ["string", "null"],
            "title": "Output Format",
            "description": "Select the output format for storing your results"
           }
        }
      }
    }
  }